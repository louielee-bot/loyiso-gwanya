# Loyiso Gwanya — GitHub Pages backup

This repository is a static, public fallback mirror of `https://louielee.zo.space/`.

The live Zo Space remains the source of truth for forms, booking, CRM, portal sessions, and APIs. The HTML captures in `live-pages/` are checked references from the live public routes.
